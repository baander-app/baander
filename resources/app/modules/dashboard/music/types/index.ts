/**
 * Music Dashboard Types
 *
 * Export all TypeScript types for music metadata operations
 */

export type {
  ArtistMetadata,
  ReleaseGroupMetadata,
  ReleaseMetadata,
  TrackMetadata,
  SearchResults,
  MetadataPreview,
  SyncConfig,
  SyncStatus,
  QueueMetrics,
  QueueJob,
  ManualMatchRequest,
  ManualMatchResponse,
} from './metadata.types';
