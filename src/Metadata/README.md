<!-- Generated by /documentation-maintainer on 2026-05-10 -->
<!-- Do not edit the generated sections above the hand-written notes line -->

# Metadata

External metadata enrichment. Reads embedded audio tags (ID3, Vorbis, FLAC, OGG, WAV), matches them against external APIs, and writes enriched data back to Catalog. Has no aggregates of its own — it operates entirely on Catalog's domain.

## Concepts

The enrichment pipeline flows: **extract** → **match** → **enrich**

- **Readers** (`Infrastructure/Reader/`) parse audio file headers and tags into `ExtractedMetadata` value objects
- **API adapters** (`Infrastructure/Api/`) wrap six external services behind internal adapters:
  Discogs, Last.fm, MusicBrainz, Spotify, TasteDive, CoverArtArchive
- **Matching** (`Infrastructure/Matching/`) compares local metadata against API results using validators to score match quality
- **Enrichers** (`Application/`) — `AlbumMetadataEnricher`, `ArtistMetadataEnricher`, `SongMetadataEnricher` — orchestrate the lookup and write-back to Catalog via port interfaces

Sync is triggered by Library dispatching `ExtractAlbumCoverCommand`, or via Messenger messages (`SyncAlbumMessage`, `SyncArtistMessage`, etc.).

## Ports

None. Metadata is a pure downstream consumer — it calls Catalog and Media ports but exposes none of its own.

## Events

None. Metadata consumes events from Library but does not publish.

## Interactions

- **Catalog** — reads and writes via `AlbumPortInterface`, `ArtistPortInterface`, `SongPortInterface`, `GenrePortInterface`
- **Library** — `CoverArtExtractor` and scanner trigger metadata extraction
- **Media** — stores cover art via `ImagePortInterface` and `StoragePortInterface`

---

<!-- Everything below this line is hand-written. Edit freely. -->

## Notes
